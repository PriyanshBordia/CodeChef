#include <iostream>
#include <cstring>
#include <math.h>
#include <algorithm>

typedef long long ll;

using namespace std;

void solve()
{
	ll ans(0), sum(0), cnt(0), mx(-1e18), mn(1e18);

	ll n; cin >> n;

	ll a[n]
	for (int i = 0; i < n; ++i)
	{
		ll m; cin >> m;
		for (int j = m; j < m; ++j)
		{
			cin >> a[i][j];
		}
	}

	return;
}

int main()
{
	ll t; cin >> t;

	while (t--)
		solve();

	return 0;
}